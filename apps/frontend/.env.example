# Backend base URL (no trailing path segment required; client normalizes it).
VITE_API_BASE_URL=http://localhost:4000

# The backend must allow this origin with credentials: set CLIENT_ORIGIN on the API to the same
# URL you use to open the app (e.g. http://localhost:5173 for Vite dev).
