[project]
name = "unilearn-ai"
version = "0.1.0"
description = "UniLearn FastAPI AI service (NLP / LLM orchestration)"
requires-python = ">=3.10"
readme = "README.md"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "httpx>=0.27.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
