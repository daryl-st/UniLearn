# Design reference screenshots

Optional: place high-fidelity mockups (PNG/WebP) here for offline reference while building screens. They are not bundled into the app unless you add them under `flutter: assets:` in `pubspec.yaml`.
